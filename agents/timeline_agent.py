"""
Timeline agent — Task 2 (Phase 3).

Reads entities + documents, builds a chronological list of clinical events.
Mostly deterministic — minimal LLM use.
"""
from __future__ import annotations


def build_timeline(entities: list[dict], documents: list[dict]) -> list[dict]:
    """STUB — Task 2 will implement. Returns empty for now."""
    return []